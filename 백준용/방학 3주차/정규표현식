// const rex = new RegExp(/\(\)|\[\]/g)

// let str = '(()())([][][]';

// str = str.replace(rex,'');
// console.log(str);

const a = [1, 2, 3, 4, 5, 6, 7, 8, 9];
const n = 5;
console.log(
  [...a.splice(0, n), 5.5, ...a]
)
